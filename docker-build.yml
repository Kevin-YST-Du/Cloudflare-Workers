version: '3.8'

services:
  proxy:
    # 本地构建
    build: .
    
    container_name: proxyx
    restart: always
    
    # 网络模式推荐 Host (性能最好，直接使用 VPS 的 21011 端口)
    # network_mode: "host"
    
    # 如果不使用 Host 模式，请取消下面 ports 的注释
    ports:
      - "21111:21111"